# Deployment configuration as it is actually shipped. Declared policy, not
# observed use — the inventory records the difference.
server {
    ssl_protocols TLSv1.1 TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-RSA-AES256-GCM-SHA384:DES-CBC3-SHA;
}

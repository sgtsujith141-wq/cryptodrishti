[pytest]
testpaths = tests
addopts = -q --strict-markers
filterwarnings =
    # Third-party deprecations we do not control; our own code must stay clean.
    ignore::DeprecationWarning:starlette.*
    ignore::DeprecationWarning:fastapi.*
    ignore::DeprecationWarning:anyio.*
